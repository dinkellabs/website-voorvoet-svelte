# SvelteKit build output
.svelte-kit
build

# Dependencies
node_modules

# Environment variables
.env
.env.*
!.env.example

# Generated Paraglide output
src/lib/paraglide

# Test coverage and reports
coverage
playwright-report
test-results
e2e/smtp-inbox.json

# Lighthouse CI working directory & local report dumps
.lighthouseci
lighthouse-reports

# Visual parity local outputs (regenerated by the manual e2e/visual-parity spec)
e2e/visual-parity/

# OS-specific
.DS_Store
Thumbs.db

# Editor-specific — keep shared VS Code workspace settings, ignore personal state
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea
*.swp
*.swo

# Claude Code / AI tooling
.claude/
CLAUDE.local.md

# Site-parity investigation artefacts (PNG diffs, JSON dumps, logs).
# Tracked docs/ reports reference these by path but the artefacts themselves
# are too large and ephemeral to commit.
artifacts/
